# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lifevault
DB_USERNAME=postgres
DB_PASSWORD=yourpassword

# JWT Configuration (must be at least 64 characters for HS512)
JWT_SECRET=your-very-long-secret-key-that-is-at-least-512-bits-which-means-64-characters-long

# Encryption Configuration (should be at least 32 characters)
ENCRYPTION_KEY=your-encryption-key-that-should-be-at-least-32-characters-long

# Email Configuration (Gmail example)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# AWS Configuration (optional)
AWS_REGION=us-east-1
SNS_ENABLED=false

# CORS Configuration
CORS_ORIGINS=http://localhost:3000

# Server Configuration
SERVER_PORT=8080